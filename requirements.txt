flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
gunicorn==22.0.0
werkzeug==3.0.3
# ASGI layer: lets `uvicorn main:app` (the default start command of most
# sandbox/PaaS builders) serve this Flask app.
uvicorn
a2wsgi
